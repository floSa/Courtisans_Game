# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
courtisans_env/

# Editor
.vscode/

# Trained models (générés par `python main.py train`).
# Forcer l'ajout d'un checkpoint particulier : `git add -f models/<file>.pth`.
models/*.pth
models/v6_checkpoints/

# Artefacts d'expériences (logs, datasets, graphes) — non versionnés.
*.log
*.npz
training_graph.png
models/*.pth.*
models/*backup*
